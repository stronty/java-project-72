# Команду setup вызывает система проверки перед запуском тестов.
# Пока она только собирает дистрибутив приложения — этого достаточно,
# чтобы убедиться, что проект собирается.
setup:
	./gradlew installDist

sonar:
	./gradlew build sonar --info